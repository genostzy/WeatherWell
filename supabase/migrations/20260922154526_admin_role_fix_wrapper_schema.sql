-- Live history records this correction as its own migration. In this repo
-- it is already folded into 20260922153905_admin_role.sql (the admin wrappers
-- are created in the public schema there), so this file only keeps the
-- version list identical to the live database's (M2).
select 1;
