-- Live history records this correction as its own migration. In this repo
-- it is already folded into 20260923071802_admin_reads_all_profiles.sql (the
-- is_admin() grant to authenticated is there), so this file only keeps the
-- version list identical to the live database's (M2).
select 1;
