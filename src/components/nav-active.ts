/**
 * The one "you are here" look for every menu and view switch: a tinted
 * fill, bold text and a solid bar along the bottom edge. A faint fill alone
 * read as hover rather than as the selected view (owner's request).
 */
export const NAV_ACTIVE = "bg-primary/15 font-semibold text-foreground shadow-[inset_0_-3px_0_0_var(--color-primary)]";
